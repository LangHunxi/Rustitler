{
  "$schema": "https://schemas.tauri.app/v2/schema.json",
  "identifier": "default",
  "description": "Default capability — minimal permissions for offline desktop app.",
  "permissions": [
    "core:default",
    "core:window:default",
    "core:path:default",
    "core:event:default",
    "dialog:allow-open"
  ]
}
